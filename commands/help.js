const helpFn = (dirPath) => {
    console.log(`
    Valid Commands
            node main.js tree "directory path"
            node main.js organize "directory path"
            node main.js help
    `);
}
module.exports = helpFn